---
"@fgladisch/pi-welcome-message": minor
---

Show the welcome message for new interactive sessions created with `/new`, add the Pi logo/current-model header, and add `welcomeMessage.showOnNewSession` plus `welcomeMessage.showLogo` settings to control the new behavior.
